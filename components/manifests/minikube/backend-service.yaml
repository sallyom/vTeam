apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: ambient-code
  labels:
    app: backend-api
spec:
  type: NodePort
  selector:
    app: backend-api
  ports:
  - port: 8080
    targetPort: http
    nodePort: 30080
    protocol: TCP
    name: http
